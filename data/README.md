# Data Documentation

This directory stores the raw and intermediate data used to build the NBA Player Role Analytics application.

Raw and processed files are excluded from Git because they are either large source files or reproducible intermediate outputs. The smaller application-ready files generated by the pipeline are committed under `static/data/`.

## Required Raw Files

Place both source files in:

```text
data/raw/